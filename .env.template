# URL de la API de Sandy Core
NEXT_PUBLIC_API_URL="http://localhost:8000"

# Microsoft Clarity
NEXT_PUBLIC_CLARITY_PROJECT_ID=""
NEXT_PUBLIC_CLARITY_ENABLE_IN_DEV="false"

# Rollbar
NEXT_PUBLIC_ROLLBAR_CLIENT_TOKEN=""
ROLLBAR_SERVER_TOKEN=""

# Twitch OAuth
NEXT_PUBLIC_TWITCH_CLIENT_ID=""
TWITCH_CLIENT_SECRET=""
NEXT_PUBLIC_REDIRECT_URI="http://localhost:3000/auth/callback"

# Stream en tiempo real (SSE y WebSocket)
# Ambos derivan de NEXT_PUBLIC_API_URL y requieren token efímero:
#   GET /stream/token (sesión de Clerk) -> GET /stream?token=... o WS /ws?token=...
# El token dura 300s y solo se valida al conectar: pedir uno nuevo en cada reconexión.
